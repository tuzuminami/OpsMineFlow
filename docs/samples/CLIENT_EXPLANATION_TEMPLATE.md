# Client Explanation Template

OpsMineFlow is a local-only OSS analysis tool for consent-based business process discovery.

The tool imports event logs, masks sensitive fields, generates process maps, and exports editable consulting artifacts such as draw.io files and Markdown reports.

Runtime external communication is prohibited by design.

