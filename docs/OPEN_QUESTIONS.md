# Open Questions

- Confirm whether the first native macOS agent should be Swift-only or Swift plus a small local helper.
- Confirm the preferred persistent store for production use: SQLite, DuckDB, or plain local JSON for the first preview.
- Confirm whether SVG export should be generated by a local renderer or deferred until Mermaid is embedded in the desktop UI.
- Confirm exact client-facing consent text after legal review.

